name: food_order_workflow
description: A single-workflow food ordering Flutter app using BLoC (mock data)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.2
  equatable: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.1.0
  mocktail: ^0.3.0
  test: any

flutter:
  uses-material-design: true
  assets:
    - assets/screenshot.png
